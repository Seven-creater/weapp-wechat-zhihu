<view class="page-container">
  <!-- 1. 顶部大图区 -->
  <view class="hero-section">
    <image class="hero-img" src="{{solution.beforeImg}}" mode="aspectFill" bindtap="previewImage" data-src="{{solution.beforeImg}}"></image>
    <view class="status-tag">{{solution.status || 'AI 分析完成'}}</view>
  </view>

  <!-- 2. 内容详情区 -->
  <view class="content-card">
    <view class="card-header">
      <view class="blue-line"></view>
      <text class="section-title">AI 智能分析报告</text>
      <view 
        class="voice-play-btn {{isPlaying ? 'playing' : ''}}"
        bindtap="toggleVoicePlay"
        aria-role="button"
        aria-label="{{isPlaying ? '停止朗读' : '朗读AI分析'}}"
      >
        <text class="voice-icon">{{isPlaying ? '🔊' : '📢'}}</text>
      </view>
    </view>
    
    <view class="report-box">
      <text class="report-text" user-select="true">{{solution.aiAnalysis}}</text>
    </view>

    <view class="info-row">
      <text class="label">📍 问题地点：</text>
      <text class="value">{{solution.address || '未知地点'}}</text>
    </view>

    <view class="user-suggestion-card" wx:if="{{solution.userSuggestion && solution.userSuggestion.length > 0}}">
      <view class="suggestion-header">
        <text class="suggestion-icon">👤</text>
        <text class="suggestion-title">我的建议</text>
      </view>
      <view class="suggestion-content">
        <text class="suggestion-text" user-select="true">{{solution.userSuggestion}}</text>
      </view>
    </view>
  </view>

  <!-- 3. 底部固定栏 (关键修复) -->
  <view class="bottom-bar">
    <!-- 左侧：收藏按钮 -->
    <view class="action-btn" bindtap="toggleCollect" aria-role="button" aria-label="{{isCollected ? '取消收藏' : '收藏'}}">
      <text class="icon">{{isCollected ? '★' : '☆'}}</text>
      <text class="btn-text">{{isCollected ? '已收藏' : '收藏'}}</text>
    </view>

    <!-- 右侧：生成公文 (主按钮) -->
    <view class="main-btn" bindtap="generateDoc" aria-role="button" aria-label="生成专业汇报材料">
      <text>生成专业汇报材料</text>
    </view>
  </view>
</view>
