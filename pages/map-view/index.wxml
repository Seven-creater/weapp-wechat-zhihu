<view class="map-container">
  <map
    class="map-view"
    latitude="{{latitude}}"
    longitude="{{longitude}}"
    scale="14"
    show-location
    markers="{{markers}}"
    bindmarkertap="handleMarkerTap"
  ></map>
</view>
